import { getPolicy, createPolicy, updatePolicy, deletePolicy } from "./api";
import { IPolicy } from "./type";

export { getPolicy, createPolicy, updatePolicy, deletePolicy };
export type { IPolicy };
